import type {PlayerModel} from "../../../types/models/PlayerModel.ts";

// note that the first field is not used (it's up to the players to decide who deals for a round)
export type TichuPlayerModel = PlayerModel;